name: Windows CI

on: [push]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
      with:
        submodules: true
    - name: Check path
      run: |
        mkdir build
        cd build
        pwd
    - name: Next
      run: pwd
    - name: Build kitty
      run: |
        mkdir build
        cd build
        cmake -G "Visual Studio 16 2019" -DKITTY_TEST=ON ..
        "C:\Program Files (x86)\Microsoft Visual Studio\2019\Enterprise\MSBuild\Current\Bin\MSBuild.exe" kitty.sln /t:run_tests
    - name: Run tests
      run: |
        cd build
        .\test\Debug\run_tests
